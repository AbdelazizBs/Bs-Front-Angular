import { PrerfixNgPipe } from './prerfix-ng.pipe';

describe('PrerfixNgPipe', () => {
  it('create an instance', () => {
    const pipe = new PrerfixNgPipe();
    expect(pipe).toBeTruthy();
  });
});
